# Y-TEC Wi-Fi Capsule 固有作業規則

## 製品境界

- 正式名称は `Y-TEC Wi-Fi Capsule`、実行ファイル名は `YtecWifiCapsule.exe` とする。
- Windows 7 SP1 / 8 / 8.1 / 10 / 11、32/64ビット両対応の.NET Framework 4.6.1 / WPFポータブルアプリとして維持する。
- 機能は保存済みWi-Fiプロファイルの選択バックアップと選択復元に限定する。
- 外部通信、クラウド同期、認証、テレメトリー、自動更新を追加しない。
- 起動直後からバックアップ／復元を操作できる画面を表示する。
- UIは日本語と英語に対応する。初期言語はWindows表示言語が日本語なら日本語、それ以外は英語とし、起動中は画面内で切り替えられる状態を維持する。

## Wi-Fiデータ

- Wi-FiプロファイルXMLとキー素材を平文ファイルへ書かない。
- Native Wi-Fi APIから取得した平文XMLはメモリ上だけで暗号化し、完了後に可能な範囲でゼロ化する。
- SSID・Wi-Fiキー・XML・PC名・ユーザー名・完全パスをログや例外詳細へ記録しない。
- 実データをテスト、スクリーンショット、リポジトリ、配布物へ含めない。
- 復元前に暗号コンテナ全体とXMLを再検証し、選択外プロファイルを登録しない。
- 既存プロファイルの上書きは既定で無効とし、利用者が画面で明示許可した場合だけ実行する。

## 暗号化

- Y-TEC Data Capsuleと同系統のAES-256-CBC / HMAC-SHA-256 Encrypt-then-MAC方式を使う。
- Wi-Fi Capsule専用のマジック、形式版、鍵素材、鍵導出ラベルを使い、他アプリとの相互復号を行わない。
- HMAC検証に成功するまで復号しない。未知の版、鍵ID、長さ、末尾追加、改ざんを拒否する。
- Y-TEC公式配布版は、リポジトリ外の32バイト公式互換鍵をビルド時にDLLリソースへ埋め込む。パスフレーズ、利用時の外部鍵ファイル、PC固有DPAPIへ勝手に変更しない。
- 公開ソースの標準ビルドは公開開発鍵を使い、実データ非推奨の警告を表示する。カスタム鍵ビルドは公式版と互換性がないことを明示する。
- 公式鍵、鍵を含む旧Git履歴、実バックアップを公開リポジトリ、ログ、Workflow Artifact、配布ZIPへ単独ファイルとして含めない。
- アプリ内蔵鍵は別PC移行を優先する方式であり、EXE・メモリを解析できる相手への秘匿性を保証しない。この制限をUI・README・脅威モデル・操作マニュアルに明記する。
- バックアップ完成ファイルを上書きせず、同じフォルダーの一意な`.partial`へ書いて完全検証後だけ完成名へ移動する。

## 検証

- 変更後はrestore、Release build、test、AnyCPU/x86/x64ビルド、ポータブル配布物検証を実行する。
- 暗号化は往復、異なる鍵、ヘッダー・暗号文・タグ改ざん、切断、末尾追加、上限、XML攻撃を合成データで確認する。
- UIは`--test-mode`の合成データだけを使い、1280×720と375px相当の狭幅で、はみ出し・重なり・クリック不能・キーボード操作・例外を確認する。
- 日本語／英語リソースは同じキー集合を持たせ、`eng/Test-Localization.ps1`と日英双方の合成UIキャプチャで参照漏れと長文の折り返しを確認する。
- 実Wi-Fiプロファイルを使うバックアップ／復元は、利用者の明示承認なしに行わない。
- Windows 7 SP1～11の保存済みVMではNICを無効のまま保ち、合成プロファイルだけで起動・暗号形式・CPUビット数を確認する。

## OSSと配布

- Y-TECが著作権を持つソースコード、文書、オリジナルアセットはApache License 2.0で公開し、`NOTICE`と第三者通知を保持する。
- 正式配布ZIPは公式鍵を埋め込んだビルドだけから作成し、公開開発鍵／カスタム鍵ビルドをY-TEC公式版として配布しない。
- 正式配布ZIPには日本語の「操作マニュアル」と英語の「User Manual」をHTML／PDFの両方で収録し、アプリは現在の表示言語に対応するマニュアルを開く。
- SignPath Foundation採択前または署名処理を利用できない配布物は未署名と明記し、SHA-256を掲載する。
